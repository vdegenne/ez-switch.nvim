# vdegenne/ez-switch.nvim

Use double `<Tab>` tap to switch to previous open buffer.

## config

```lua
return {
  "vdegenne/ez-switch.nvim",
  config = function()
    require("ez-switch").setup()
  end,
}
```

That's all, (but probably not enough.)
